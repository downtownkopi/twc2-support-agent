services:
  support:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      # Bind to localhost only — Cloudflare/edge proxy (Caddy/nginx) terminates
      # TLS and forwards here. Never publish on 0.0.0.0.
      - "127.0.0.1:5099:5099"
